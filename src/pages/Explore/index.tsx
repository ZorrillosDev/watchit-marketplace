// react imports
import React from 'react';

// ===========================|| EXPLORE ||=========================== //

const Explore = (): JSX.Element => {
  return <span>Explore</span>;
};

export default Explore;
